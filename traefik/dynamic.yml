http:
  routers:
    app:
      rule: 'Host(`{{env "HOSTNAME"}}`)'
      service: app
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt

  services:
    app:
      loadBalancer:
        servers:
          - url: "http://app:8080"
