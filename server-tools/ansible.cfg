[defaults]
# roles/ holds the roles specific to this project. external/ is the checkout of
# dhis2-sre/server-baseline that `make roles` produces, and is gitignored: bootstrap, firewall and
# harden live there now.
roles_path = ./roles:./external/server-baseline/roles

# You can adjust this number based on how many hosts you have
forks = 10

# Enable persistent SSH connections for faster execution
ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o ConnectTimeout=5 -o ServerAliveInterval=60 -o Compression=yes

# Command timeout in seconds
timeout = 30

interpreter_python = /usr/bin/python3
